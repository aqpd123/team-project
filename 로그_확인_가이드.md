# 로그 확인 가이드

## 1. 백엔드 Flask 서버 로그 확인

### 방법 1: PowerShell 터미널에서 확인
1. `start_backend.ps1` 스크립트를 실행한 PowerShell 창을 확인합니다
2. 이 창에 모든 Flask 서버 로그가 실시간으로 출력됩니다
3. 예상되는 로그 예시:
   ```
   📥 사용자 1 데이터 직렬화 시작
   📥 사용자 1 saju_data 존재 여부: True
   ✅ 사용자 1 saju_data JSON 파싱 성공
   📤 사용자 1 직렬화 완료, saju_analysis 존재: True
   ```

### 방법 2: 별도 터미널에서 확인
만약 백엔드 서버가 백그라운드에서 실행 중이라면:
```powershell
# 프로젝트 루트 디렉토리에서
cd C:\Users\user\Documents\TeamProject
.\.venv311\Scripts\Activate.ps1
py -m flask run --host 0.0.0.0 --port 5000
```

---

## 2. 프론트엔드 Flutter 앱 로그 확인

### 방법 1: Flutter 실행 터미널에서 확인
1. Flutter 앱을 실행한 터미널/명령 프롬프트 창을 확인합니다
2. 예: `flutter run -d 192.168.0.5:5555` 명령을 실행한 창
3. 이 창에 모든 Flutter 앱 로그가 출력됩니다
4. 예상되는 로그 예시:
   ```
   🔍 사용자 응답 전체: {user_id: 1, username: test, ...}
   🔍 saju_analysis 타입: _Map<String, dynamic>
   🔍 saju_analysis 값: {character: wood, five: {...}, ...}
   ```

### 방법 2: Android Studio Logcat에서 확인
1. Android Studio를 엽니다
2. 하단의 **Logcat** 탭을 클릭합니다
3. 필터에서 "flutter" 또는 앱 패키지 이름으로 필터링합니다
4. `print()` 문으로 출력된 로그를 확인할 수 있습니다

### 방법 3: VS Code에서 확인
1. VS Code에서 Flutter 앱을 실행한 경우
2. 하단의 **Debug Console** 또는 **Terminal** 탭에서 확인합니다

---

## 3. 로그 확인 체크리스트

### 백엔드에서 확인할 항목:
- [ ] `🔍 GET /users/{user_id} 요청 받음` - API 요청이 도착했는지
- [ ] `📥 사용자 X saju_data 존재 여부: True/False` - 데이터베이스에 데이터가 있는지
- [ ] `✅ 사용자 X saju_data JSON 파싱 성공` - JSON 파싱이 성공했는지
- [ ] `📤 saju_analysis 필드 존재: True/False` - 응답에 필드가 포함되었는지

### 프론트엔드에서 확인할 항목:
- [ ] `🔍 사용자 응답 전체: {...}` - API 응답이 도착했는지
- [ ] `🔍 saju_analysis 값: {...}` - saju_analysis 필드가 있는지
- [ ] `🔍 saju_analysis가 null인가: false` - null이 아닌지
- [ ] `✅ 사주 분석 결과 파싱 성공` - 파싱이 성공했는지

---

## 4. 문제 해결 팁

### 백엔드 로그가 보이지 않는 경우:
1. 백엔드 서버가 실행 중인지 확인
2. `start_backend.ps1`을 다시 실행
3. PowerShell 창이 닫혔는지 확인

### 프론트엔드 로그가 보이지 않는 경우:
1. Flutter 앱이 실행 중인지 확인
2. `flutter run` 명령을 다시 실행
3. Android Studio의 Logcat에서 확인

### 로그가 너무 많은 경우:
- 백엔드: 특정 사용자 ID로 필터링 (예: `사용자 1`)
- 프론트엔드: `🔍` 또는 `❌` 이모지로 검색

---

## 5. 로그 예시

### 정상적인 경우:
```
[백엔드]
🔍 GET /users/1 요청 받음
📥 사용자 1 데이터 직렬화 시작
📥 사용자 1 saju_data 존재 여부: True
✅ 사용자 1 saju_data JSON 파싱 성공
📤 사용자 1 직렬화 완료, saju_analysis 존재: True

[프론트엔드]
🔍 사용자 응답 전체: {user_id: 1, username: test, saju_analysis: {...}}
🔍 saju_analysis 값: {character: wood, five: {...}, ...}
✅ 사주 분석 결과 파싱 성공
```

### 문제가 있는 경우:
```
[백엔드]
⚠️ 사용자 1 saju_data가 None이거나 비어있음
또는
❌ 사용자 1 saju_data JSON 파싱 실패: ...

[프론트엔드]
⚠️ 사주 분석 결과가 없습니다
또는
❌ 사주 분석 결과 파싱 실패: ...
```

